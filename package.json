{
  "version": "0.1.0",
  "private": true,
  "license": "ISC",
  "packageManager": "yarn@3.2.0",
  "workspaces": [
    "js/cli",
    "js/lib",
    "rust",
    "tests"
  ],
  "scripts": {
    "format": "turbo run format",
    "format:check": "turbo run format:check",
    "lint": "turbo run lint",
    "lint:check": "turbo run lint:check",
    "build": "turbo run build",
    "build:ci": "turbo run build:ci",
    "build:cli": "turbo run build --filter @raindrops-protocol/raindrops-cli",
    "build:lib": "turbo run build --filter @raindrops-protocol/raindrops",
    "build:program": "turbo run build --filter @raindrops-protocol/raindrops-programs",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@metaplex-foundation/amman": "^0.10.0",
    "turbo": "^1.4.3"
  }
}
